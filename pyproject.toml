[project]
name = "methdup"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vmduarut", email = "victor.dr95@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pysam>=0.24.0",
]

[project.scripts]
methdup = "methdup:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.5",
]
